// Кто такой грэдл ? (мэйвэнн pom.xml - устаревший.) Преимущество грэдла в том,что он написан на  Groovy(ЯП) а мэйвэн на xml (язык разметки)
// основное преимущество в том,что обьем кода в грэдле меньше и читать проще  и удобнее. + структура проще
// в этом файле конфиг проекта. Работает не трогай. Не создавать себе гемороя
// selenide - проект на гитхабе.
// maven central -
plugins {
    id 'java-library'
}
repositories {
    mavenCentral()
}

dependencies {
    testImplementation()
}
